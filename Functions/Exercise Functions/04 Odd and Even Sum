single_number = input()

def odd_even(n):
    odd = 0
    even = 0
    for i in n:
        if int(i) % 2 != 0:
            odd += int(i)
        else:
            even += int(i)
    return (f"Odd sum = {odd}, Even sum = {even}")

print(odd_even(single_number))
